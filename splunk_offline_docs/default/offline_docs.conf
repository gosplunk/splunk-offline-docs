# Splunk Offline Docs — default configuration
# Override in local/offline_docs.conf or via the Configuration page (local/settings.json)

products = enterprise,es8,soar,itsi
rate_limit = 0.35
python = /usr/bin/python3
scraper_root =
daily_check_enabled = false
